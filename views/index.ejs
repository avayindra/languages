<%- include("lout/heds") %>
<main>
  <form action="/" method="post">
    <input type="text" placeholder="programming language" name="planguage" />
    <button type="submit">Add</button>
  </form>
  <ul>
    <%if(plNames){ %> <%for(let x=0; x< plNames.length;x++){%>
    <li><%= plNames[x]%></li>
    <% }%> <% }else{ %>
    <p>No data found</p>
    <% } %>
  </ul>
</main>

<%- include("lout/footer") %>
